Date 3/6/2026
I created the React enviroment integrated tailwind
and started on the first section which was the nav bar 
1. Created the DIV Container for the navbar
2. added links, logo, and contact button
3. finished by adding color and integrating google fonts to tailwind

Date 3/10/2026
I inputed the hero and about us section design into copilot sonnet 4.5 for the hero and opus for the about us section
it drafted up pretty good sections for both but had some minor issues
1. for the hero section it gave a oretty good inital design just messed up the colors andspacing a but nothing a little css tweaking couldnt fix
2. for the about us it kept structuring everything pretty badly espcially when spacing out the columns but we worked on adding better margin spacing and a grid so now everything works 👍
3. lastly i ended up tweaking with the navbar i made it sticky so that it would stay at the top of the page always
and on desktop itll shrink about 50 percent of its size and on mobile it has a scroll on and off feature
lastly i just tweaked the color of the display menu on mobile for the navbar and made it blur 

Date 3/23/2026
Worked on the Services section of the website.
1. Created the services component with 4 service panels: Comercial, Residential, Industrial, and Emergency
2. On desktop, the panels are side by side — hovering on a panel slides it open revealing the service image background, title, and description. When not hovering, all panels stay closed with dark navy covers
3. On mobile, implemented a sticky scroll feature — the section stays pinned as the user scrolls and each service panel opens one at a time, then unsticks once all services have been revealed
4. Added the service background image behind dark navy overlays that fade away when each panel is active
5. Added vertical service name labels on collapsed panels (desktop) and horizontal labels on mobile
6. Added borders between panels to make them visually distinct
7. Changed fonts: SERVICES title uses Karantina, service numbers use Karantina, and the content title/description inside each panel uses Alexandria